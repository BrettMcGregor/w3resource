# Write a Python program that accepts a sequence of lines
# (blank line to terminate) as input and prints the lines as
# output (all characters in lower case)?

multi_line_string = "Write a Python program that accepts a sequence of lines\n" \
                    "(blank line to terminate) as input and prints the lines as\n" \
                    "output (all characters in lower case)?"

print(multi_line_string.upper())
